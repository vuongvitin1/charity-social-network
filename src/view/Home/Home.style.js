import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    pagination: {
        float: 'right',
        margin: 'auto',
    },
    spinner: {
        textAlign: 'center',
    }
}));